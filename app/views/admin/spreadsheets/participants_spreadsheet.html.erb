<table id="participants-spreadsheet" class="spreadsheet">

  <thead>
    <tr>
      <th>Name</th>
      <th>Andrew ID</th>
      <th>Department</th>
      <th>Class Level</th>
      <th>Status</th>
    </tr>
  </thead>

  <tbody>
    <% @participants.each do |p| %>
    <tr>
      <td><% unless p.name.nil? %><%= p.name %><% end %></td>
      <td><% unless p.andrewid.nil? %><%= p.andrewid %><% end %></td>
      <td><%= sanitize [p.department].join(',<br/>'), :tags => %w(br) %></td>
      <td><% unless p.student_class.nil? %><%= p.student_class %><% end %></td>
      <td>
	<%= link_to p.registrations.size, '#' %>
	<div class="reg" style="display: none">
	  <% p.registrations.each do |r| %>
	  <%= r.seminar.title %> (<%= r.status %>)<br/>
	  <% end %>
	</div>
      </td>
    </tr>
    <% end %>
  </tbody>

</table>
