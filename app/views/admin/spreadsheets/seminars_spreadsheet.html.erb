<table id="seminars-spreadsheet" class="spreadsheet">

  <thead>
    <tr>
      <th>Title</th>
      <th>Status</th>
      <th>Description</th>
    </tr>
  </thead>

  <tbody>
    <% @seminars.each do |s| %>
    <tr>
      <td><%= s.title %></td>
      <td><%= s.status %></td>
      
      <td class="truncate">
	<div class="preview">
	  <%= truncate s.description, :length => 39 %>
	</div>
	<div class="truncated">
	  <%= s.description %>
	  <%= link_to 'hide', '#', :class => 'hide-text' %>
	</div>
      </td>
    </tr>
    <% end %>
  </tbody>

</table>
