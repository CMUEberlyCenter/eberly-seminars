$("a.hide-if-enrolled-in-future-faculty").
     addClass( "success" ).
     attr( "href",
           "<%= escape_javascript ungraduate_participant_future_faculty_program_url @participant %>" ).
     html("Graduated! Undo?").
     delay(10000).
     fadeOut();
