$('#navigation').fadeOut(
  100,
  function(){ $(this).html( "<%= escape_javascript( render( "layouts/utility_nav" ) ) %>" ).fadeIn( 300 ); }
);

$('.sidebar-note').html( "<%= content_for?( :sidebar_note) ? escape_javascript( content_for( :sidebar_note ) ) : "" %>");

$('.header-content').fadeOut(
  300,
  function(){ $(this).html( "<%= escape_javascript( header_content ) %>" ).fadeIn( 300); }
);


$('#content').fadeOut(
  100,
  function(){ $(this).html( "<%= escape_javascript( render( "registrations" ) ) %>" ).fadeIn(300); }
);